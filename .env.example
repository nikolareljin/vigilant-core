NEWS_API_KEY=
OLLAMA_MODEL=qwen2.5:7b
OLLAMA_HOST=http://localhost:11434
# Optional: force UI timestamps to a specific IANA timezone (default is host local time)
# Examples: America/New_York, Europe/Belgrade, UTC
DISPLAY_TIMEZONE=
# Legacy fallback also supported (used only if DISPLAY_TIMEZONE is unset):
# TIMEZONE=
